[package]
name = "uplink-updater-tauri"
version = "0.9.1"
edition = "2021"
description = "Uplink 更新平台 Tauri 宿主 Rust 侧（@uplink/updater-sdk/tauri 配套：动态端点 + deviceId + minisign 验签命令集）"
license = "UNLICENSED"
publish = false

[dependencies]
tauri = "2"
tauri-plugin-updater = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"
